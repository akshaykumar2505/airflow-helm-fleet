namespace: pipelines
helm:
  releaseName: "airflow"
  chart: "airflow"
  repo: "https://airflow-helm.github.io/charts"
  version: "8.8.0"
  disablePreProcess: false
  force: false
  timeoutSeconds: 0
  valuesFiles:
    - values.yaml  
